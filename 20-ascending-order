public Boolean ascendingOrder(Integer a, Integer b, Integer c) {
    //code here
    if(b <= a || c <=b){
        return false;
    }
    else if(a == b || b == c || c == a){
         return true;
    }
    else{
        return true;
    }
}