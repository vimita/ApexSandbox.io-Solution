public Boolean companionPlants(String plant1, String plant2) {
    Boolean isCompanion = false;

    if (plant1 == 'onions') {
        if (plant2 == 'lettuce' || plant2 == 'carrots' || plant2 == 'tomatoes') {
            isCompanion = true;
        }
    } else if (plant1 == 'lettuce') {
        if (plant2 == 'cucumbers' || plant2 == 'onions') {
            isCompanion = true;
        }
    } else if (plant1 == 'cucumbers') {
        if (plant2 == 'lettuce') {
            isCompanion = true;
        }
    } else if (plant1 == 'carrots') {
        if (plant2 == 'onions') {
            isCompanion = true;
        }
    } else if (plant1 == 'tomatoes') {
        if (plant2 == 'onions') {
            isCompanion = true;
        }
    }

    return isCompanion; // Moved the return statement outside the if-else blocks
}
